# ============================================================
# DeepSeekFS – Desktop Edition  (replaces requirements.txt)
# Install:  pip install -r requirements-desktop.txt
# ============================================================

# ── Desktop UI (replaces pywebview) ──────────────────────────────
PyQt6>=6.6.0

# ── ML core (unchanged from v1.0) ───────────────────────────────
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4
numpy>=1.24.0
scikit-learn>=1.3.0

# ── File ingestion (unchanged) ─────────────────────────────────
python-docx>=0.8.11
PyMuPDF>=1.23.5
pdfminer.six>=20221105
openpyxl>=3.1.2
python-pptx>=0.6.21

# ── Utilities (unchanged) ─────────────────────────────────────
watchdog>=3.0.0
dateparser>=1.1.8
orjson>=3.9.0
joblib>=1.3.0
python-dotenv>=1.0.0

# ── Build tool (needed only to make a .exe) ───────────────────────
pyinstaller>=6.4.0

# ── NOT needed any more (web-only, removed) ───────────────────────
# fastapi
# uvicorn
# pywebview
# python-multipart
# pydantic

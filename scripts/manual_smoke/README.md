# Manual smoke scripts

These scripts are useful for operator-led checks, but they are not part of the
normal automated pytest suite.

- `llm_load_smoke.py`: local model load and simple generation smoke test
- `v52_modules_smoke.py`: import-level module smoke test
- `v52_tools_smoke.py`: tool execution smoke test

services:
  deepseekfs:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
    volumes:
      - deepseekfs_storage:/app/storage
    restart: unless-stopped

volumes:
  deepseekfs_storage:
